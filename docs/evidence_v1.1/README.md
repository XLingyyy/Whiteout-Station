# v1.1 UI evidence

These 1280×720 captures were generated by the Unreal runtime after the v1.1
gameplay and UI integration:

- `UI_opening_story_09_1280x720.png`: opening text declares 12 AP split across
  morning, afternoon, and dusk.
- `UI_hud_1280x720.png`: the left HUD lists several current phase choices.
- `UI_guide_1280x720.png`: the handbook explains phase AP, heating, and
  alternative strategies.
- `UI_settings_llm_1280x720.png`: provider, BaseURL, session-only API key,
  preset model, custom model ID, and enable/apply controls fit at 720p.

Packaged real-input and Shipping evidence is generated separately under the
release artifact's `Validation` directory.
