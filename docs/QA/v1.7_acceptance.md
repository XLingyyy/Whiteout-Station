# v1.7 验收覆盖说明

测试人：Codex。实现基线 `c9b8435`；首轮已打包回归源码 `5131d44`，导航文字换行修正源码 `4394e2d`。全部执行记录位于本机 `Artifacts/v1.7-evidence/`。机器为 i7-13650HX、16 GB RAM、RTX 4060 Laptop，UE 5.8，Windows。

## 已执行范围

| 施工用例 | 实际检查与证据 | 结论范围 |
| --- | --- | --- |
| T01、T07、T08、G01 | 游戏窗口中按原生 Slate 键盘事件导航五页；逐页比较完整 FWSGameState、StateRevision 和角色位置；完成后检查暂停、移动和视角锁 | 渲染集成通过；OS 实际鼠标按键另列 |
| T02、T04、T05 | 流程单测覆盖开场按住、两帧释放门控、重复释放、过快导航、失焦取消及禁用上一页；渲染集成覆盖禁用上一页与按键 repeat | 状态机通过；鼠标命中与父子控件路由未据此宣称通过 |
| T06 | 渲染窗口中 Esc 请求跳过、Esc 取消并保持原页 | 取消流程通过；OS 点击确认跳过待补 |
| T10、T15 | 手册重看返回原层和原滚动位置，Completed 保持；30 次重看关闭后无残留租约，进程正常退出 | 原生集成通过；独立的手动切关卡未测试 |
| T11、T12、T13 | 流程单测检查 InProgress 稳定页 ID、Completed/Skipped 不重复、已读游戏档不强制打断；真实旧档迁移检查 | 策略和持久化相关检查通过；跨应用重启阅读、对话/结果/供暖中旧档的逐场景 OS 验收待补 |
| T14 | 代码使用 IsPresentationModalSafe 查询在途对话、提交与生命周期状态；重看入口不取消请求 | 已实现；未在本轮对真实在途在线请求做窗口验收 |
| A01、A02、A04、A06 | AP 模型检查阶段 4/3/2/1/0、2 AP 斜纹、超预算不截断、0 AP 不着色；v1.6 额度/零 AP 定向测试 | 定向自动化通过 |
| A03、A07 | 实际 PreviewAction、取消、CommitAction、重复提交反馈通知 | 渲染集成通过 |
| A05、A08、A09 | 十路线跨阶段和费用结算、独立新槽/旧档读取；保留既有确认前校验 | 路线与存档通过；各个 UI 动效场景未全部逐一人工验收 |
| S01、S02、S03 | Status.Permissions 单测；实机玩家与 NPC 四字段、未知伤势配图检查 | 信息权限定向测试与截图通过；所有危险阈值窗口切换未穷举 |
| S04、G02 | 十条 v1.6 保留路线完整导出比对，除随机事务 ID 外全部字段一致 | 通过，包含状态、资源、消耗、事件、任务和评分 |
| 损坏新槽 | 四字节文件、完整文件中间字节翻转、文件截断；随后恢复正常文件读取 | Editor / Shipping 均通过，无隐式旧槽回退 |
| 图像资源 | 七张纹理、六条模式记录均为 Approved；真实图片来源/裁剪/哈希；Shipping 运行时加载 | 通过 |
| 资产保护 | 262 个地图/NPC/材质 SHA256 与开工记录一致 | 通过，仅按项目保护要求执行一次发布复核 |

## 窗口与真人验收的限制

最终 `4394e2d` 候选的教程布局矩阵已完成：720p/1080p 各三档文字，1440p 和超宽屏各一档，共 8 组、40 张截图。每组五页导航和锁释放等 11 项检查通过。视觉抽查覆盖八种配置，记录在 `layout-matrix.json` 和 `frames/`；这项结果不扩展为所有旧菜单的全分辨率人工验收。

本轮原生事件运行在真实 GPU 游戏窗口中，截图来自 FScreenshotRequest，未以 NullRHI 代替渲染证明。桌面自动化曾返回与 UE 窗口身份不一致的前台应用画面，激活 UE 也失败，因此停止向桌面发送输入，避免干扰用户正在使用的应用。

尚未签收的项目包括 OS 鼠标命中/长按/双击/点击穿透、真实 Alt-Tab 返回、Tab 焦点可见性、中文 IME 输入与草稿，以及上表所列的逐场景人工操作。现有原生事件和代码检查不等同于这些 OS 用例通过。

3–5 名陌生玩家理解测试尚未进行，需要实际玩家参与；重点记录交谈入口、白色轮廓、AP 与体能区别、“尚未确认”的含义和第一次预览/取消操作。不能用自动化或代理阅读代替该项。

因此本轮产物保留为候选包，不替换已验证的 v1.6 包，不宣称施工文档所有 P0/P1 已签收。
