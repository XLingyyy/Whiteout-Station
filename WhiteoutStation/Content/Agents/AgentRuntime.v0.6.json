{
  "schema_version": 2,
  "runtime_version": "0.6.0",
  "provider_name": "deepseek",
  "model": "deepseek-v4-flash",
  "endpoint": "https://api.deepseek.com/chat/completions",
  "timeout_seconds": 8,
  "llm_enabled": false,
  "notes": "Online dialogue expression is opt-in. Credentials are read only from WHITEOUT_LLM_API_KEY or ignored LocalConfig/WhiteoutLLM.ini; only the official DeepSeek host receives Authorization and AutoRoute remains offline."
}
